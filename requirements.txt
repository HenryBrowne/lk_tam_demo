# Portfolio Signal — pinned dependencies
# All versions verified/frozen on 2026-09-04 (Python 3.12, Windows).
# requirements.lock.txt holds the full transitive freeze for exact reproduction.

# --- LiveKit real-time stack ---
livekit-agents==1.7.1
livekit-api==1.2.1
livekit-plugins-deepgram==1.7.1
livekit-plugins-cartesia==1.7.1
livekit-plugins-anthropic==1.7.1
livekit-plugins-openai==1.7.1          # kept only for the documented all-OpenAI fallback
livekit-plugins-silero==1.7.1          # VAD for the STT->LLM->TTS pipeline
livekit-plugins-turn-detector==1.7.1   # end-of-utterance model (feeds eou_ms)

# --- Webhook receiver ---
fastapi==0.141.1
uvicorn[standard]==0.52.4

# --- Account brief (LLM) ---
anthropic==1.3.0

# --- Dashboard ---
streamlit==1.63.0
plotly==7.0.0
pandas==3.0.5

# --- Config / util ---
pyyaml==6.0.3
python-dotenv==1.2.3
